# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
tmp-assets/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
.DS_Store?
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sensitive files
.env
.repomixignore
food-pantry.code-workspace

# Data
*.db
*.db-shm
*.db-wal
*.db-journal
data/

# Defense in depth for legacy repository-local paths. Operational source data
# belongs outside the repository (see FEED_PRIVATE_DATA_DIR); never publish it.
/docs/OFB_Orders.xlsx
/docs/reports/OFB_*.csv

# Real operational exports and historical records may contain sensitive
# organization data and must never be committed or published.
/docs/reports/RealData/

# Generated local artifacts
output/
tmp/
# Runtime storage only — anchored to repo root so these do NOT match the
# source directory packages/backend/src/services/storage/
/storage/
/packages/backend/storage/

# Claude Code local state (worktrees, projects, sessions)
.claude/
